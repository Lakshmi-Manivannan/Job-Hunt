<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jobhuntapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User.RequestedServices"></activity>
        <activity android:name=".User.SignUp2" />
        <activity android:name=".ServiceProvider.AddRequest" />
        <activity android:name=".ServiceProvider.RequestService" />
        <activity android:name=".ServiceProvider.AllServiceProvider" />
        <activity android:name=".User.UserDashboard" />
        <activity android:name=".User.VerificationPhone" />
        <activity
            android:name=".User.Profile"
            android:label="@string/title_activity_profile" />
        <activity android:name=".User.SignUp" />
        <activity android:name=".Common.Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".User.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>